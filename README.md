# Customer Churn Prediction

🚧 **Development in Progress**

Active development is happening on the `dev` branch.

The `main` branch will contain the first stable release of the end-to-end
customer churn prediction system, including data preprocessing, modeling,
evaluation, and explainability.

Please switch to the `dev` branch to view the current implementation.
